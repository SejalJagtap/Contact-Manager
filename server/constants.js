exports.constants = {
    // Generates a 400 Bad Request error
    badRequest: 400,

    // Generates a 401 Unauthorized error
    unauthorized:401,

    // Generates a 403 Forbidden error
    forbidden:403,

    // Generates a 404 Not Found error
    notFound:404

    // Optionally, add other HTTP error methods as needed
};
